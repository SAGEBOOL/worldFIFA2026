## 2026-07-01 14:35 全站功能修复

- **问题**：自动化任务 8718379 更新赛事数据时，由于 prompt 中只写了更新 DAYS/SCORERS/ODDS，实际上完整替换了整个 index.html 文件，导致大量功能代码（renderDay、renderScorers、renderOdds、renderStandingsMatrix、TEAM_SPOTLIGHT、WIKI_DATA 等）被删除
- **根因**：自动化脚本只从 API 拉取比赛数据，然后完整重写整个文件，覆盖了手动编写的所有渲染函数和模态框代码
- **修复方案**：从可靠提交 1c87d1b（姆巴佩修正版）恢复 index.html，该版本包含完整的前端渲染逻辑
- **验证**：7个板块ID全部存在，像素时钟HTML/CSS/JS全部就位，DAYS/SCORERS_FULL/ODDS/TEAMS_SPOTLIGHT/VENUES常量完整
- **自动化 prompt 修复**：添加了 `**CRITICAL**: 不要完整重写 index.html，只能修改特定常量的值` 的警告
